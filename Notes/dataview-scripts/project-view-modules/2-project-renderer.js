/**
 * Отрисовывает сообщение о загрузке. Эта функция вызывается немедленно в начале процесса обновления,
 * чтобы предоставить пользователю мгновенную обратную связь. Это улучшает пользовательский опыт,
 * так как вместо пустого пространства или мерцания старых данных пользователь видит понятное
 * сообщение о том, что информация в данный момент обновляется.
 * @param {HTMLElement} container - Родительский HTML-элемент, в который будет добавлено сообщение.
 */
function renderLoading(container) {
    const loadingMessage = container.createEl('p', { text: '🔄 Обновление данных...' });
    loadingMessage.style.textAlign = 'center';
    loadingMessage.style.color = 'var(--text-muted)';
    loadingMessage.style.padding = '2em 0';
}

/**
 * Основная функция отрисовки, отвечающая за отображение финального результата. Она принимает
 * подготовленный массив данных от сборщика и рендерит его в виде таблицы с помощью API Dataview.
 * Функция также обрабатывает случай, когда данные отсутствуют, и выводит вместо таблицы
 * соответствующее сообщение, что делает интерфейс более понятным для пользователя.
 * @param {object} dv - Объект API Dataview, переданный из основного скрипта.
 * @param {HTMLElement} container - Родительский HTML-элемент для рендеринга.
 * @param {Array<object>} data - Финальный массив объектов с данными о задачах, полученный от сборщика.
 */
function renderTable(dv, container, data) {
    // Проверяем, есть ли в массиве хотя бы один элемент для отображения.
    if (data.length) {
        // Если данные есть, используем встроенный метод dv.table для создания таблицы.
        // Мы передаем ему массив заголовков и двумерный массив строк, который получаем,
        // преобразуя наш массив объектов с помощью метода map.
        dv.table(
            ["Заметка", "Инстанс", "Kanban", "Статус", "Время выполнения"],
            data.map(d => [d.note, d.instance, d.kanbanBoards, d.status, d.executionTime])
        );
    } else {
        // Если массив данных пуст, выводим информативное сообщение.
        dv.paragraph("Нет данных для отображения.");
    }
}

// "Экспортируем" функции, чтобы их можно было загрузить и вызвать в основном скрипте project-view.js.
return { renderLoading, renderTable };